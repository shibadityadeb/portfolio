import React from 'react';

const Contact: React.FC = () => {
  return (
    <div className="contact-section">
      {/* Contact component placeholder - can be expanded later */}
    </div>
  );
};

export default Contact;